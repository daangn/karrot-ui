import type { KeyframeDefinition } from "@seed-design/qvism-core";

export function defineKeyframes(
  definition: Record<string, KeyframeDefinition>,
): Record<string, KeyframeDefinition> {
  return definition;
}
