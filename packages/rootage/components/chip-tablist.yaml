# yaml-language-server: $schema=./schema.json
kind: ComponentSpec
metadata:
  id: chip-tablist
  name: Chip Tablist
data:
  base:
    enabled:
      root:
        paddingX: $dimension.s4
  variant=neutral solid:
    enabled:
      root:
        gap: 0px
  variant=brand solid:
    enabled:
      root:
        gap: 8px
