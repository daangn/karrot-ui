# yaml-language-server: $schema=./schema.json
kind: ComponentSpec
metadata:
  id: chip-tablist
  name: Chip Tablist
data:
  base:
    enabled:
      root:
        paddingX: $unit.s4
  variant=neutral solid:
    enabled:
      triggerList:
        gap: 0px
  variant=brand solid:
    enabled:
      triggerList:
        gap: 8px
